<?php echo $this->getContent() ?>

<div align="center" class="well">

    <form class="form-search" method="post">

        <div align="left">
            <h3>Fogot your password?</h3>
        </div>

        <?php echo $form->render("email") ?>
        <?php foreach ($form->getMessagesFor('email') as $message) { echo '
        <p>',$message, '</p>'; } ?>

        <?php echo $form->render("Remind password") ?>
        <?php echo $form->render("csrf", array("value" => $this->security->getToken())) ?>
        <?php foreach ($form->getMessagesFor('csrf') as $message) { echo '
        <p>',$message, '
        </p>'; } ?>

        <hr />

    </form>

</div>
